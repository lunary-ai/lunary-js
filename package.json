{
  "name": "llmonitor",
  "version": "0.2.3",
  "description": "LLMonitor is an open-source logging and analytics platform for LLM-powered apps.",
  "main": "lib/index.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=neutral --outfile=lib/index.js && npm run types",
    "types": "tsc --emitDeclarationOnly"
  },
  "author": "vincelwt",
  "type": "module",
  "license": "ISC",
  "prettier": {
    "semi": false
  },
  "devDependencies": {
    "esbuild": "^0.17.19",
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "langchain": "^0.0.102",
    "p-queue": "^7.3.4"
  }
}
